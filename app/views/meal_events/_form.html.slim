/ Mobile: single column, Desktop: 2/3 + 1/3
.grid.lg:grid-cols-3.gap-4.sm:gap-8
  / Main form
  .lg:col-span-2
    = form_with model: @meal_event, class: "space-y-4 sm:space-y-6" do |f|
      - if @meal_event.errors.any?
        .card.bg-red-50.border-red-200.p-4.sm:p-6.mb-4.sm:mb-6
          .flex.items-start.gap-3.sm:gap-4
            .w-8.h-8.sm:w-10.sm:h-10.bg-red-100.rounded-xl.flex.items-center.justify-center.text-red-500.flex-shrink-0
              span ⚠️
            div
              h3.font-semibold.text-red-800.mb-1.sm:mb-2.text-sm.sm:text-base
                = "#{@meal_event.errors.count} erreur(s)"
              ul.text-xs.sm:text-sm.text-red-600.space-y-1
                - @meal_event.errors.full_messages.each do |message|
                  li = message

      .card.p-4.sm:p-8.space-y-4.sm:space-y-6
        h2.text-lg.sm:text-xl.font-display.font-bold.text-navy.flex.items-center.gap-2.sm:gap-3.mb-1.sm:mb-2
          span 🎉
          | Informations

        .space-y-4.sm:space-y-5
          .form-group
            = f.label :title, class: "form-label text-sm sm:text-base"
              span Nom de l'événement
              span.text-terracotta.ml-1 *
            = f.text_field :title, required: true, class: "form-input text-base", placeholder: "Ex: Anniversaire de Sophie"

          .form-group
            = f.label :date, class: "form-label text-sm sm:text-base"
              span Date
              span.text-terracotta.ml-1 *
            .relative
              = f.date_field :date, required: true, class: "form-input text-base", min: Date.today
              .absolute.right-3.sm:right-4.top-1/2.-translate-y-1/2.text-navy/40.pointer-events-none
                | 📅

          .form-group
            = f.label :guests_count, class: "form-label text-sm sm:text-base"
              | Nombre d'invités
            .relative
              = f.number_field :guests_count, min: 0, class: "form-input pl-10 sm:pl-12 text-base", placeholder: "10"
              .absolute.left-3.sm:left-4.top-1/2.-translate-y-1/2.text-navy/40
                | 👥
            p.text-xs.sm:text-sm.text-navy/50.mt-2 Optionnel — pour planifier les quantités

      / Actions
      .grid.grid-cols-2.sm:flex.sm:flex-row.gap-3.sm:gap-4.pt-2.sm:pt-4
        = f.submit class: "btn btn-primary col-span-2 sm:flex-none justify-center"
        = link_to "Annuler", (@meal_event.persisted? ? meal_event_path(@meal_event) : meal_events_path), class: "btn btn-outline col-span-2 sm:col-span-1 justify-center"

  / Sidebar (hidden on mobile)
  .hidden.lg:block.space-y-6
    .card.p-6.bg-gradient-to-br.from-mustard/20.to-mustard/5
      .flex.items-center.gap-3.mb-4
        .w-10.h-10.bg-mustard/30.rounded-xl.flex.items-center.justify-center
          span 💡
        h3.font-semibold.text-navy Idées d'événements

      ul.space-y-3.text-sm.text-navy/70
        li.flex.items-center.gap-2
          span 🎂
          | Anniversaires
        li.flex.items-center.gap-2
          span 🎄
          | Fêtes de fin d'année
        li.flex.items-center.gap-2
          span 🍖
          | Barbecue entre amis
        li.flex.items-center.gap-2
          span 🥂
          | Dîner romantique
        li.flex.items-center.gap-2
          span 👨‍👩‍👧‍👦
          | Repas de famille

    .card.p-6
      .flex.items-center.gap-3.mb-4
        .w-10.h-10.bg-sage/20.rounded-xl.flex.items-center.justify-center
          span 📝
        h3.font-semibold.text-navy Astuce

      p.text-sm.text-navy/70
        | Une fois l'événement créé, vous pourrez planifier les repas depuis la page de planification.
