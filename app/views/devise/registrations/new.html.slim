.min-h-screen.flex.items-center.justify-center.py-8.sm:py-12.px-4
  .w-full.max-w-md

    / Logo & Welcome
    .text-center.mb-6.sm:mb-8
      .w-16.h-16.sm:w-20.sm:h-20.bg-gradient-to-br.from-sage.to-sage-dark.rounded-xl.sm:rounded-2xl.flex.items-center.justify-center.mx-auto.mb-4.sm:mb-6.shadow-xl
        span.text-3xl.sm:text-4xl 👨‍👩‍👧‍👦
      h1.font-display.font-bold.text-2xl.sm:text-3xl.text-navy.mb-1.sm:mb-2 Rejoignez-nous !
      p.text-navy/60.text-sm.sm:text-base Créez votre compte familial

    / Registration form
    .card.p-6.sm:p-8
      = form_for(resource, as: resource_name, url: registration_path(resource_name), html: { class: "space-y-4 sm:space-y-6" }) do |f|
        = render "devise/shared/error_messages", resource: resource

        .form-group
          = f.label :email, class: "form-label text-sm sm:text-base"
            | Email
            span.text-terracotta.ml-1 *
          = f.email_field :email, autofocus: true, autocomplete: "email", class: "form-input text-base", placeholder: "votre@email.com"

        .form-group
          = f.label :password, class: "form-label text-sm sm:text-base"
            | Mot de passe
            span.text-terracotta.ml-1 *
          = f.password_field :password, autocomplete: "new-password", class: "form-input text-base", placeholder: "••••••••"
          - if @minimum_password_length
            p.text-xs.sm:text-sm.text-navy/50.mt-2
              | Minimum #{@minimum_password_length} caractères

        .form-group
          = f.label :password_confirmation, class: "form-label text-sm sm:text-base"
            | Confirmer le mot de passe
            span.text-terracotta.ml-1 *
          = f.password_field :password_confirmation, autocomplete: "new-password", class: "form-input text-base", placeholder: "••••••••"

        = f.submit "Créer mon compte", class: "btn btn-primary w-full text-base sm:text-lg py-2.5 sm:py-3"

    / Login link
    .text-center.mt-4.sm:mt-6
      p.text-navy/60.text-sm.sm:text-base
        | Déjà un compte ?
        = link_to "Se connecter", new_session_path(resource_name), class: "text-terracotta font-semibold hover:underline ml-1"

    / Features preview
    .mt-8.sm:mt-12.grid.grid-cols-3.gap-2.sm:gap-4.text-center
      .p-2.sm:p-4
        .text-xl.sm:text-2xl.mb-1.sm:mb-2 📖
        .text-xs.text-navy/50 Créez vos recettes
      .p-2.sm:p-4
        .text-xl.sm:text-2xl.mb-1.sm:mb-2 🗓️
        .text-xs.text-navy/50 Planifiez vos repas
      .p-2.sm:p-4
        .text-xl.sm:text-2xl.mb-1.sm:mb-2 🎉
        .text-xs.text-navy/50 Organisez vos fêtes
